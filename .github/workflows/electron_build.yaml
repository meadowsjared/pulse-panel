name: Build and Release Pulse Panel

on:
  push:
    branches:
      - main  # Set this to the branch you want to build from

jobs:
  build:
    runs-on: windows-latest  # Specifies that the job should run on the latest Windows runner

    steps:
      - uses: actions/checkout@v3  # Checks out your repository under $GITHUB_WORKSPACE

      - name: Set up Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: 'latest'

      - name: Install dependencies with Bun
        run: bun install

      - name: Build Electron app with Bun
        run: |
          bun run build -p never

      - name: Extract Version
        run: echo "::set-output name=app_version::$(cat package.json | jq -r '.version')"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: my-electron-app
          path: ./release/**/*

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # GitHub automatically creates this secret to use in your workflow
        with:
          tag_name: ${{ steps.extract_version.outputs.app_version }}
          release_name: Pulse Panel ${{ steps.extract_version.outputs.app_version }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./release/${{ steps.extract_version.outputs.app_version }}/pulse-panel_${{ steps.extract_version.outputs.app_version }}.exe
          asset_name: pulse-panel_${{ steps.extract_version.outputs.app_version }}.exe
          asset_content_type: application/octet-stream
